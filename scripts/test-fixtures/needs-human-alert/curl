#!/usr/bin/env bash
set -euo pipefail

: "${NOTIFY_CURL_ARGUMENTS_FILE:?NOTIFY_CURL_ARGUMENTS_FILE is required}"
printf '%s\n' "$@" > "$NOTIFY_CURL_ARGUMENTS_FILE"

if [ "${NOTIFY_CURL_SCENARIO:-success}" = "failure" ]; then
    exit 22
fi
